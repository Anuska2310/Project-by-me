from quote import quote
import random
print("Enter the name of the author:")
q_author=input()
quotes=quote(q_author)
quote_no=random.randint(2,len(quotes))
print("Author:",quotes[quote_no]['author'])
print("",quotes[quote_no]['quote'])